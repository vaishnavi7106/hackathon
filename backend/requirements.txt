fastapi==0.115.5
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg2-binary==2.9.12

# Redis
redis==5.2.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Settings
pydantic==2.10.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# HTTP clients (for external APIs)
httpx==0.28.1

# Web Push notifications (VAPID)
pywebpush>=2.0.0

# LLM — provider-agnostic client (see app/services/llm_client.py)
google-genai==1.16.0
groq>=1.4.0

# google-generativeai not needed — google-genai (above) covers Gemini Vision
# Optional future providers (uncomment to enable):
# anthropic==0.40.0
# openai==1.57.0
# ollama==0.4.4

# File handling
python-multipart==0.0.18
aiofiles==24.1.0
Pillow==11.0.0

# Utilities
python-dateutil==2.9.0
haversine==2.9.0

# Scheduling — Pillar 5 outbreak detection cron
apscheduler==3.10.4

# Logging
structlog==24.4.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
anyio[trio]>=4.8.0

# Pillar 1 — Crop Sentinel ML
torch==2.9.0
torchvision==0.24.0
# shap==0.46.0       # optional: heatmap generation (install when py3.14 wheels available)
# matplotlib==3.9.2  # optional: heatmap generation

# Pillar 3 — Market price forecasting (LightGBM models + pipeline)
lightgbm>=4.0.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.26.0
requests>=2.31.0
optuna>=3.0.0twilio>=9.0.0
